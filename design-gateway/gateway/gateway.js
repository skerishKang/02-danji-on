/* DanjiOn two-surface preview landing (#403). Navigation is static and fully relative. */
(function () {
  'use strict';
  // Keep this entrypoint for the existing gateway build contract. Root IA is
  // deliberately static: the registry belongs inside HISTORY, not at root.
})();
