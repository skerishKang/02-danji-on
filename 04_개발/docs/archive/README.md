# Archived status/handoff documents

These documents are point-in-time handoff, verdict, status-lock, and
P0-AUTHZ execution artifacts. They are preserved here for history; they are
not the current authority for any live behavior.

Archive criteria (#375 F3):
- dated handoff/closeout/reconciliation reports,
- CTO resume/verdict snapshots,
- the completed P0_AUTHZ execution record (status, checklists, scope,
  sandbox evidence).

Current authority stays in `../`:
- contract and policy documents (`*_v1.md`, `*_v2.md`, `CURRENT_*`),
- `LIVE_INTEGRATION_RELEASE_GATE_v1.md` (referenced by
  `.github/workflows/live-release-gate.yml`),
- `v2/` (referenced by `.github/workflows/v2-integration-gate.yml`),
- documents pinned by backend contract tests.
