# Gate1 live account binding

This visual layer calls the Better Auth client only when `VITE_AUTH_MODE=danjion`.

Development preview remains non-mutating. Account authentication does not grant resident authorization.
