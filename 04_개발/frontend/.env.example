# mock: v5 in-memory fixture data
# api: call the Cloudflare Worker API
VITE_DATA_MODE=mock

# Same-origin by default. Set only when frontend/API are served separately in development.
VITE_API_BASE_URL=
VITE_COMPLEX_SLUG=bangnim-myeongji-roadhill

# Development only. adapter.ts sends this header only when import.meta.env.DEV is true.
# The backend still resolves the role from database fixtures; this is not an admin-role header.
VITE_DEV_AUTH_USER=dev-resident-001
